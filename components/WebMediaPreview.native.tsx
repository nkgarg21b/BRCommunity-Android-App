import React from 'react';

type Props = {
  url: string;
  title: string;
  soundEnabled: boolean;
  soundRequest: number;
  onEnableSound: () => void;
};

export function WebMediaPreview(_props: Props) {
  return null;
}